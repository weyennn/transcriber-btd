**Spec Compliance** - **Task 5 (Frontend disable tombol AI):** ⚠️ — Patch verbatim sesuai brief (src/web/static/app.js:40-51); badge + disable + tooltip benar. Namun ada **lubang fungsional di luar verbatim snippet**: `endJob()` (app.js:195) dan catch handler notulen (app.js:318) meng-set `btnAINotulen.disabled = false` secara tidak kondisional, sehingga setelah transkripsi pertama selesai, tombol AI **re-enable sendiri** meski `AI_ENABLED=false`. Disable dari `checkEnv` hanya bertahan sampai job pertama selesai. Klaim report "persis verbatim" akurat, tapi "disable tombol saat flag off" tidak  …(+3389 chars)
